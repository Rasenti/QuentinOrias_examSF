{% extends 'base.html.twig' %}

{% block title %}Édition{% endblock %}

{% block body %}

    <h1 class="m-3 text-center">Éditer les informations de l'Utilisateur</h1>

    <div class="container-fluid w-50 mt-5">
        <div class="d-flex flex-column align-items-stretch">

            {{ include('admin/_form.html.twig', {'button_label': 'Éditer'}) }}
            <a href="{{ path('app_admin_index') }}"><button class="btn btn-primary">Retour à la liste</button></a>
            {{ include('admin/_delete_form.html.twig') }}

        </div>
    </div>

{% endblock %}
