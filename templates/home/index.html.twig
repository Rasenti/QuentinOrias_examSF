{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    
    <h1 class="m-4 text-center">🪓 Bienvenue sur AirHache 🪓</h1>
    <h2 class="m-4 text-center">Retrouvez ici la liste de tous les employés !</h1>
    <div class="container-fluid row justify-content-center vw-75">

        {% for user in users %}  
            <div class="card m-2 p-1 col-2">
                <img src="
                    {% if 'https://' in user.picture %}
                        {{ user.picture }}
                    {% else %}
                        {{ asset('uploads/'~ user.picture) }}
                    {% endif %}
                " class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">{{ user.firstname }} {{ user.lastname }}</h5>
                    <p class="card-text">{{user.sector}}</p>
                    <p class="card-text">{{user.email}}</p>
                    <a href="{{ path('app_details', {'id': user.id}) }}" class="btn btn-info">Détails</a>
                </div>
            </div>
        {% endfor %}

    </div>
{% endblock %}
